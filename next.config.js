/** @type {import('next').Config} */
const nextConfig = {
  basePath: '/wave-poser',
  // Remove webpack config for now to work with Turbopack
  // TODO: Add back webpack config if needed for TensorFlow.js
}

module.exports = nextConfig
